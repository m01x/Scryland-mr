{
  "name": "scryland-mr",
  "version": "0.0.0",
  "private": true,
  "description": "Scryland Mono Repo : Personal Card Stock Fetcher.",
  "engines": {
    "node": ">=24"
  },
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "dev:api": "pnpm --filter @scryland/api start:dev",
    "dev:web": "pnpm --filter @scryland/web dev",
    "dev": "concurrently -k -n api,web \"pnpm dev:api\" \"wait-on http://localhost:3000/api/health && pnpm dev:web\"",
    "build:shared": "pnpm --filter @scryland/shared build",
    "build": "pnpm build:shared && pnpm --filter @scryland/api build && pnpm --filter @scryland/web build"
  },
  "devDependencies": {
    "concurrently": "^10.0.5",
    "wait-on": "^9.1.0"
  }
}
